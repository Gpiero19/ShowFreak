# Plan: Add Atomic Commit Enforcement to AGENTS.md

## Goal
Add mandatory atomic commit enforcement rule to `worktrees/backend/AGENTS.md`.

## Task

| ID | Action |
|----|--------|
| R-01 | Add atomic commit enforcement section to AGENTS.md |

## Implementation

Add to end of `worktrees/backend/AGENTS.md`:

```
### ATOMIC COMMIT ENFORCEMENT (MANDATORY)

Every completed task MUST result in:
1. A git commit in the correct worktree
2. One commit per task (no batching multiple INF tasks)
3. Commit must reference EXACT task ID

Forbidden:
- grouping multiple INF tasks in one commit
- "initial scaffold" commits covering multiple tasks
- runtime-only excuses without classification

Allowed exception:
- database creation (INF-03) → documented as runtime event in commit message
```

## Execution
- Edit `worktrees/backend/AGENTS.md` - append enforcement section
- Create commit: `backend: R-01 - Add atomic commit enforcement rule`