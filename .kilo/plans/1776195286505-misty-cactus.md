# Phase 1: Infrastructure Setup (INF tasks)

## Goal
Execute INF-01 through INF-07 in the backend worktree to establish the backend foundation.

## Tasks (Backend Worktree)

| Task ID | Task | Dependencies | Worktree |
|---------|------|--------------|-----------|
| INF-01 | Initialize Node.js project with TypeScript | None | backend |
| INF-02 | Install backend dependencies (express, prisma, jsonwebtoken, bcrypt, zod, cors, dotenv) | INF-01 | backend |
| INF-03 | Create PostgreSQL database "showfreak" | None | backend |
| INF-04 | Configure Prisma schema with all models | INF-02, INF-03 | backend |
| INF-05 | Run Prisma migration and generate client | INF-04 | backend |
| INF-06 | Configure environment variables (.env) | INF-01 | backend |
| INF-07 | Create Express app structure (app.ts, server.ts) | INF-02 | backend |

## Execution
- Run all tasks in `worktrees/backend` on `feature/backend` branch
- Follow dependency order strictly

## Frontend Worktree
- Remains idle (on `feature/frontend` branch)
- Will start Phase 4 (FE-01) when prompted

## Verification
- Backend server runs and connects to PostgreSQL database